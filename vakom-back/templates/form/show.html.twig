{% extends 'base.html.twig' %}

{% block title %}Form{% endblock %}

{% block body %}
    <h1>Form</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ form.nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ form.mail }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ form.ville }}</td>
            </tr>
            <tr>
                <th>Societe</th>
                <td>{{ form.societe }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ form.objet ? form.objet|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ form.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_index') }}">back to list</a>

    <a href="{{ path('form_edit', {'id': form.id}) }}">edit</a>

    {{ include('form/_delete_form.html.twig') }}
{% endblock %}
